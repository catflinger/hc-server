import "mocha";
import { expect } from "chai";

describe("config-manager", () => {
    describe("getConfig", () => {
        it("should exist", () => {
            expect(true).to.be.true;
        });
    });
});